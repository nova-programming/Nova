def test_func(n) {
    print("Called test_func with " + str(n))
    if n <= 1 { return n }
    res = test_func(n - 1) + 100
    print("Returning " + str(res))
    return res
}
def run() {
    print("test(2) = " + str(test_func(2)))
}
run()

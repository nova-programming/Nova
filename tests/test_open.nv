import os_win

fd = sys_open("build_test.nv", "r")
if fd == 0 {
    print("fd is null")
    sys_exit(1)
}
print("opened")
content = sys_read(fd)
print(content)
print("---")
print("read " + str(len(content)))
sys_close(fd)
